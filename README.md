# Court Scanner 🎾

Court Scanner is a web application that displays available tennis courts for **today only**, helping users quickly find playable time slots without manually checking each venue’s booking system.

The app scans participating venues and highlights courts with consecutive availability, making it easy to see usable booking windows at a glance.

---

## 🚀 Running the Project

### Backend (Required)

The backend must be started locally.

1. Navigate to the backend folder:

```bash
cd backend
```

2. Install dependencies (if you haven’t already):

```
npm install
```

3. Start the backend server:

```
npm run start
```

### Backend
The frontend is deployed and accessible at:
[Link Text](https://court-scanner-seven.vercel.app/)
